import { ClickOutDirectiveDirective } from './click-out-directive.directive';

describe('ClickOutDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
